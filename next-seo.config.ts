import type { DefaultSeoProps } from "next-seo";

const config: DefaultSeoProps = {
  title: "HPG Newspaper",
  description: "HPG Newspaper. Газета с последними новостями по ХПГ 3.0",
  openGraph: {
    type: "website",
    locale: "ru_RU",
  },
};

export default config;
